stages:
  - lint
  - publish
  - meta

lint-all:
  stage: lint
  before_script:
    - dotnet tool update -g aiursoft.apkg.client --configfile ./nuget.config || dotnet tool install -g aiursoft.apkg.client --configfile ./nuget.config
    - export PATH="$PATH:$HOME/.dotnet/tools"
    - apkg --version
  script:
    - |
      failed=0
      for dir in */; do
        if ls "$dir"*.aosproj >/dev/null 2>&1; then
          echo "::group::Linting $dir"
          (cd "$dir" && apkg lint) || failed=1
          echo "::endgroup::"
        fi
      done
      exit $failed
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH != "master"

.publish:
  stage: publish
  before_script:
    - dotnet tool update -g aiursoft.apkg.client --configfile ./nuget.config || dotnet tool install -g aiursoft.apkg.client --configfile ./nuget.config
    - export PATH="$PATH:$HOME/.dotnet/tools"
    - apkg --version
  script:
    - cd $PACKAGE_DIR
    - apkg publish
    - APKG_FILE=$(ls bin/*.apkg 2>/dev/null | head -1)
    - |-
      if [ "$CI_COMMIT_BRANCH" = "prod" ]; then
        apkg push -f "$APKG_FILE" -s "https://apkg.aiursoft.com" -k "$APKG_PROD_API_KEY" --skip-duplicate
      elif [ "$CI_COMMIT_BRANCH" = "master" ]; then
        apkg push -f "$APKG_FILE" -s "https://apkg-dev.aiursoft.com" -k "$APKG_DEV_API_KEY" --skip-duplicate
      fi
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH == "master"
    - if: $CI_COMMIT_BRANCH == "prod"

base-files:
  extends: .publish
  variables:
    PACKAGE_DIR: base-files

plymouth-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: plymouth-anduinos

anduinos-apt-config:
  extends: .publish
  needs:
    - anduinos-archive-keyring
  variables:
    PACKAGE_DIR: anduinos-apt-config

anduinos-gdm3-wallpaper:
  extends: .publish
  needs:
    - anduinos-wallpapers
  variables:
    PACKAGE_DIR: anduinos-gdm3-wallpaper

anduinos-apt-config-dev:
  extends: .publish
  needs:
    - anduinos-archive-keyring
  variables:
    PACKAGE_DIR: anduinos-apt-config-dev

anduinos-appearance:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-appearance

anduinos-no-snapd:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-no-snapd

anduinos-fonts:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-fonts

anduinos-archive-keyring:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-archive-keyring

anduinos-bwrap-hack:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-bwrap-hack

anduinos-fluent-gtk-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-fluent-gtk-theme

anduinos-fluent-icon-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-fluent-icon-theme

anduinos-dconf-defaults:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-dconf-defaults

anduinos-deskmon:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-deskmon

gnome-shell-extension-anduinos-loc:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-anduinos-loc

gnome-shell-extension-no-power-icon:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-no-power-icon

gnome-shell-extension-noti-bottom-right:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-noti-bottom-right

gnome-shell-extension-accent-gtk-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-accent-gtk-theme

gnome-shell-extension-accent-user-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-accent-user-theme

gnome-shell-extension-accent-icons-theme:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-accent-icons-theme

gnome-shell-extension-appindicator-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-appindicator-anduinos

gnome-shell-extension-arcmenu:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-arcmenu

gnome-shell-extension-blur-my-shell:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-blur-my-shell

gnome-shell-extension-clipboard-indicator:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-clipboard-indicator

gnome-shell-extension-customize-ibus:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-customize-ibus

gnome-shell-extension-dash-to-panel-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-dash-to-panel-anduinos

gnome-shell-extension-desktop-icons-ng-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-desktop-icons-ng-anduinos

gnome-shell-extension-lockkeys:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-lockkeys

gnome-shell-extension-mediacontrols:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-mediacontrols

gnome-shell-extension-network-stats:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-network-stats

gnome-shell-extension-proxy-switcher:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-proxy-switcher

gnome-shell-extension-simple-weather:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-simple-weather

gnome-shell-extension-tiling-assistant:
  extends: .publish
  variables:
    PACKAGE_DIR: gnome-shell-extension-tiling-assistant

anduinos-wallpapers:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-wallpapers

alsa-ucm-conf-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: alsa-ucm-conf-anduinos

firmware-sof-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: firmware-sof-anduinos

anduinos-theme:
  extends: .publish
  stage: meta
  needs:
    - anduinos-fluent-icon-theme
    - anduinos-fluent-gtk-theme
    - anduinos-gnome-extensions
    - anduinos-dconf-defaults
    - plymouth-anduinos
    - anduinos-wallpapers
    - anduinos-gdm3-wallpaper
    - anduinos-gnome-shell-locale
    - anduinos-fonts
    - anduinos-appearance
  variables:
    PACKAGE_DIR: anduinos-theme

anduinos-software-properties-common:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-software-properties-common

anduinos-software-properties-gtk:
  extends: .publish
  needs:
    - anduinos-software-properties-common
  variables:
    PACKAGE_DIR: anduinos-software-properties-gtk

anduinos-system-tweaks:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-system-tweaks

anduinos-system-tweaks-server:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-system-tweaks-server

anduinos-templates:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-templates

anduinos-gnome-shell-locale:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-gnome-shell-locale

anduinos-rime:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-rime

anduinos-live-settings:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-live-settings

anduinos-installer-config:
  extends: .publish
  needs:
    - anduinos-live-settings
    - anduinos-bwrap-hack
    - anduinos-rime
  variables:
    PACKAGE_DIR: anduinos-installer-config

# ── Onion-layer metapackages ─────────────────────────────────────────────
# anduinos-core-system: no internal deps (pure Ubuntu packages)
# anduinos-desktop-core: depends on core-system
# anduinos-appstore:       Flatpak + GNOME Software (optional for servers)

anduinos-core-system:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-core-system

anduinos-desktop-core:
  extends: .publish
  stage: meta
  needs:
    - anduinos-core-system
    - anduinos-session
    - firmware-sof-anduinos
    - alsa-ucm-conf-anduinos
    - anduinos-rime
  variables:
    PACKAGE_DIR: anduinos-desktop-core

anduinos-appstore:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-appstore

# ── Meta-packages: must wait for their dependencies ──────────────────────

anduinos-gnome-extensions:
  extends: .publish
  stage: meta
  needs:
    - gnome-shell-extension-anduinos-loc
    - gnome-shell-extension-no-power-icon
    - gnome-shell-extension-noti-bottom-right
    - gnome-shell-extension-accent-gtk-theme
    - gnome-shell-extension-accent-user-theme
    - gnome-shell-extension-accent-icons-theme
    - gnome-shell-extension-appindicator-anduinos
    - gnome-shell-extension-arcmenu
    - gnome-shell-extension-blur-my-shell
    - gnome-shell-extension-clipboard-indicator
    - gnome-shell-extension-customize-ibus
    - gnome-shell-extension-dash-to-panel-anduinos
    - gnome-shell-extension-desktop-icons-ng-anduinos
    - gnome-shell-extension-lockkeys
    - gnome-shell-extension-mediacontrols
    - gnome-shell-extension-network-stats
    - gnome-shell-extension-proxy-switcher
    - gnome-shell-extension-simple-weather
    - gnome-shell-extension-tiling-assistant
  variables:
    PACKAGE_DIR: anduinos-gnome-extensions

anduinos-session:
  extends: .publish
  needs:
    - anduinos-mimeapps
  variables:
    PACKAGE_DIR: anduinos-session

anduinos-mimeapps:
  extends: .publish
  variables:
    PACKAGE_DIR: anduinos-mimeapps

firefox-anduinos:
  extends: .publish
  variables:
    PACKAGE_DIR: firefox-anduinos

anduinos-desktop-apps:
  extends: .publish
  stage: meta
  needs:
    - anduinos-mimeapps
    - firefox-anduinos
  variables:
    PACKAGE_DIR: anduinos-desktop-apps

# ── Ultimate meta-package ──────────────────────────────────────────────────

anduinos-desktop:
  extends: .publish
  stage: meta
  needs:
    - anduinos-desktop-core
    - anduinos-appstore
    - anduinos-apt-config
    - anduinos-apt-config-dev
    - anduinos-deskmon
    - base-files
    - anduinos-theme
    - anduinos-templates
    - plymouth-anduinos
    - anduinos-software-properties-common
    - anduinos-software-properties-gtk
    - anduinos-system-tweaks
    - anduinos-no-snapd
    - anduinos-desktop-apps
  variables:
    PACKAGE_DIR: anduinos-desktop
